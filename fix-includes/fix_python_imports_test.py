"""TODO(csilvers): add these tests:

1)
import json
import zlib
import xml.etree.ElementTree

and make sure it keeps xml with the system imports.

2)
import mymodule
try:
   import simplejson
except ImportError:
   import json

and add in newmodule, and make sure newmodule goes below mymodule, not
at the top of the file.

3) Have a top of file docstring but no import, and add an import and
make sure it goes after the docstring.
"""
